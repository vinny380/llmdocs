# Tests and coverage — use with a dev venv:
#   python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt && .venv/bin/pip install -e .
#
-r requirements.txt
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.28.1
